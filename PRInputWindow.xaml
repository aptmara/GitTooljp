<Window x:Class="SimplePRClient.PRInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Pull Request" Height="300" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Title:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="TitleTextBox" Margin="0,0,0,10" Padding="3" x:FieldModifier="public"/>

        <TextBlock Grid.Row="2" Text="Body:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" x:Name="BodyTextBox" Margin="0,0,0,10" Padding="3" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" x:FieldModifier="public"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Create PR" Width="80" Click="OkButton_Click" IsDefault="True" Margin="0,0,10,0" Padding="5"/>
            <Button Content="Cancel" Width="70" IsCancel="True" Padding="5"/>
        </StackPanel>
    </Grid>
</Window>
