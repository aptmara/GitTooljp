<Window x:Class="SimplePRClient.DependencyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimplePRClient"
        mc:Ignorable="d"
        Title="必要なツールの確認" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="DescriptionStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="LineHeight" Value="22"/>
        </Style>
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E1E4E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icon / Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="🛠️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="セットアップが必要です" Style="{StaticResource HeaderStyle}" HorizontalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="このアプリケーションを実行するには、以下のツールが必要です。未インストールの項目があるため、準備をお願いします。" Style="{StaticResource DescriptionStyle}" TextAlignment="Center"/>

        <!-- Missing Tools List -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center">
            
            <!-- Git Section -->
            <Border Style="{StaticResource CardStyle}" x:Name="GitPanel" Visibility="Visible">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Git (バージョン管理システム)" FontWeight="Bold" FontSize="15" Margin="0,0,0,5"/>
                        <TextBlock Text="リポジトリの操作に必須です。" Foreground="#666"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="ダウンロードページを開く" Click="DownloadGit_Click" Background="#2EA44F" Foreground="White" BorderThickness="0"/>
                </Grid>
            </Border>

            <!-- GitHub CLI Section -->
            <Border Style="{StaticResource CardStyle}" x:Name="GhPanel" Visibility="Visible">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="GitHub CLI (gh)" FontWeight="Bold" FontSize="15" Margin="0,0,0,5"/>
                        <TextBlock Text="Pull Requestの作成などに使用します。" Foreground="#666"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="ダウンロードページを開く" Click="DownloadGh_Click" Background="#2EA44F" Foreground="White" BorderThickness="0"/>
                </Grid>
            </Border>
            
            <TextBlock Text="※ インストール後は、この画面の「再確認」ボタンを押してください。" Foreground="#888" FontSize="12" Margin="5,10,0,0" FontStyle="Italic"/>
            <TextBlock Text="※ 反映されない場合は、アプリケーションを再起動してください。" Foreground="#888" FontSize="12" Margin="5,2,0,0" FontStyle="Italic"/>
        </StackPanel>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="再確認 (Reload)" Click="Reload_Click" Width="150" Background="#0366D6" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
            <Button Content="閉じる" Click="Close_Click" Width="100" Margin="15,5,0,5" Background="Transparent" BorderBrush="#CCC" BorderThickness="1"/>
        </StackPanel>
    </Grid>
</Window>
